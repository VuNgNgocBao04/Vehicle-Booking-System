@{
    ViewData["Title"] = "Access denied";
}

<div class="container py-5" style="max-width: 640px;">
    <h1 class="h3 mb-3">Access denied</h1>
    <p class="text-muted mb-4">You do not have permission to access this resource.</p>

    <div class="d-flex gap-2">
        <a class="btn btn-primary" asp-controller="Home" asp-action="Index">Back to home</a>
        <a class="btn btn-outline-secondary" asp-controller="Account" asp-action="Login">Login with another account</a>
    </div>
</div>